<div class="property-card">
  <div class="property-left">
    <% if property.photos.first %>
      <p><%= cl_image_tag (property.photos.first).path, width: 100, height: 100, crop: :fill %></p>
    <% end %>
    <div class="info">
      <p>Cod.<%= property.id %> • <%= property.address.neighborhood %> • <%= property.address.city %></p>
      <p><%= property.address.street %>, <%= property.address.number  %></p>
      <p><%= (number_to_currency property.value.value.floor, unit: "R$", delimiter: ".", precision: 0) if property.value.value != nil %></p>
    </div>
  </div>

  <div class="bussiness-type">
    <p><b><%= %></b></p>
  </div>
  <%= link_to "", admin_property_path(property), class: "property-link" %>
</div>
